extends authorized

block content
  div.container.text-center
    h1 United States Statutory Law Interpretation
  div.container.text-center
    form(enctype="application/x-www-form-urlencoded" method="POST" action="/usslinterpret")
      input(type="text" name="query" size="40" placeholder="Enter a term of interest")
      button(type="submit") Interpret
  if query
    div.container
      if decisions.length > 0
        p Showing results for "#{query}":
        ol
          each entry in decisions
            li
              a(href=entry.url)= entry.title
              ul
                each sentence in entry.sentences
                  li= sentence
      else
        p No results for "#{query}".
